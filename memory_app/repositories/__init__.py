"""数据访问层(repositories)。

═══════════════════════════════════════════════════════════════════════════════
分层职责
═══════════════════════════════════════════════════════════════════════════════
- ``mongo_repo``    SOT 源头真相;MemCell 主存储
- ``es_repo``       BM25 检索索引(写入热路径)
- ``milvus_repo``   向量检索索引(写入热路径)
- ``dlq``           三库同步失败时的降级队列

业务平面(IngestService)依赖这些 repo 的**接口**(鸭子类型),不依赖具体后端。
便于:
- 测试用 fake / in-memory 替换
- 写入热路径+ 切换 KVStore = ``mongo`` → ``pg`` 等
"""
