"""数据访问层(repositories)。

═══════════════════════════════════════════════════════════════════════════════
分层职责
═══════════════════════════════════════════════════════════════════════════════
- ``mongo_repo``    SOT 源头真相;MemCell 主存储(§5.2)
- ``es_repo``       BM25 检索索引(Phase 2.3)
- ``milvus_repo``   向量检索索引(Phase 2.3)
- ``dlq``           三库同步失败时的降级队列(§5.4)

业务平面(IngestService)依赖这些 repo 的**接口**(鸭子类型),不依赖具体后端。
便于:
- 测试用 fake / in-memory 替换
- Phase 2+ 切换 KVStore = ``mongo`` → ``pg`` 等
"""
